export function getAISuggestion() {
  return {
    problem: "Users are not clicking the buy button",
    suggestions: [
      "Make button bigger",
      "Change color to green",
      "Add urgency text like '50% OFF'"
    ]
  };
}